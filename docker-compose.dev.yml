version: "3.9"

services:
  api:
    ports:
      - "8000:8000"
    volumes:
      - .:/app   # Mount code for hot reload
    command: uvicorn career_assistant.api.main:app --host 0.0.0.0 --port 8000 --reload
